/**
 * Contains all the Event's Contants for ngx-forms
 * @access public
 */
export class Events {
    /**
     * Message when select form tab
     * @type {string}
     */
    public static readonly SELECT_FORM_TAB = 'selectFormTab';

}
